import 'package:flutter/material.dart';

class VisitorPhotoScreenMobile extends StatelessWidget {
  const VisitorPhotoScreenMobile({super.key});

  @override
  Widget build(BuildContext context) {
    var size = MediaQuery.of(context).size;
    return Scaffold();
  }
}
